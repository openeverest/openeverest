---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: monitoringconfigs.monitoring.openeverest.io
spec:
  group: monitoring.openeverest.io
  names:
    kind: MonitoringConfig
    listKind: MonitoringConfigList
    plural: monitoringconfigs
    singular: monitoringconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Monitoring tool type (e.g., pmm)
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Indicates if any Instance uses the monitoring config
      jsonPath: .status.inUse
      name: InUse
      type: boolean
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: MonitoringConfig is the Schema for the monitoringconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MonitoringConfig
            properties:
              pmm:
                description: |-
                  PMM contains PMM-specific monitoring configuration.
                  Required when type is "pmm".
                properties:
                  credentialsSecretName:
                    description: |-
                      CredentialsSecretName is the reference to the secret containing the API key.
                      It contains `apiKey` key with the API key value.
                    type: string
                  url:
                    description: URL is the URL of the PMM server.
                    type: string
                  verifyTLS:
                    default: true
                    description: |-
                      VerifyTLS is set to ensure TLS/SSL verification.
                      If unspecified, the default value is true.
                    type: boolean
                required:
                - credentialsSecretName
                - url
                type: object
              type:
                description: Type is the name of monitoring tool (e.g., "pmm").
                enum:
                - pmm
                type: string
            required:
            - type
            type: object
            x-kubernetes-validations:
            - message: pmm config is required when type is pmm
              rule: self.type != 'pmm' || has(self.pmm)
            - message: pmm config is only allowed when type is pmm
              rule: '!has(self.pmm) || self.type == ''pmm'''
          status:
            default:
              inUse: false
            description: status defines the observed state of MonitoringConfig
            properties:
              inUse:
                default: false
                description: InUse is a flag that indicates if any Instance uses the
                  monitoring config.
                type: boolean
              lastObservedGeneration:
                description: LastObservedGeneration is the most recent generation
                  observed for this MonitoringConfig.
                format: int64
                type: integer
              pmm:
                description: PMM contains PMM-specific status information.
                properties:
                  serverVersion:
                    description: ServerVersion shows the PMM server version.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
