apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-plugin.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "my-plugin.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "my-plugin.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
